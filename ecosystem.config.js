export default {
  apps: [{
    // 🏷️ 1. Идентификация приложения
    name: "fate-bot",
    script: "dist/index.js",
    cwd: "/var/www/fate-bot",  // Явно указываем рабочую директорию
    
    // 🔧 2. Режим работы (изучаем разницу)
    instances: 1,              // Один инстанс - для бота достаточно
    exec_mode: "fork",         // "fork" vs "cluster" - важная концепция!
    // cluster_mode не нужен боту, но изучи на будущее для веб-серверов
    
    // 🛡️ 3. Надежность (ключевая DevOps концепция)
    autorestart: true,         // Автоматический перезапуск при падении
    watch: false,              // В продакшене выключаем watch
    
    // 💾 4. Управление ресурсами (мониторинг)
    max_memory_restart: "150M", // Бот легкий, 150MB - безопасно
    // Научись анализировать: pm2 monit, pm2 show
    
    // ⏱️ 5. Graceful shutdown (профессиональные практики)
    kill_timeout: 5000,        // 5 секунд на graceful shutdown
    // Telegram бот должен успеть завершить текущие запросы
    
    // 📝 6. Логирование (основа мониторинга)
    error_file: "/var/log/fate-bot/error.log",
    out_file: "/var/log/fate-bot/out.log",
    log_file: "/var/log/fate-bot/combined.log",
    log_date_format: "YYYY-MM-DD HH:mm:ss",
    time: true,                // Временные метки для анализа
    
    // 🌍 7. Переменные окружения (DevOps практика)
    env: {
      NODE_ENV: "development",
      DEBUG: "bot:*"           // Для отладки
    },
    env_production: {
      NODE_ENV: "production",
      // Изучи 12-factor app: конфиг в окружении, а не в коде
    },
    
    // 🔍 8. Для отладки и обучения
    node_args: "--trace-warnings",  // Видеть все предупреждения
    max_restarts: 10,          // Ограничить бесконечные перезапуски
    min_uptime: "10s",         // Что считать "стабильной работой"
    
    // 🎯 9. Дополнительно для обучения
    vizion: true,              // Позволяет PM2 отслеживать git изменения
    post_update: ["npm install"], // Автоматизация после деплоя
    
  }]
};